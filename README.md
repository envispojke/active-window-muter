# active-window-muter
 A simple, low-overhead utility to toggle the audio of your currently active window (game, browser, media player, etc).   The script is optimized for gaming and is anti-cheat safe (when used per my recommendations below).  It features a customizable popup UI that doesn't steal window focus, even from fullscreen/borderless applications. 
